[project]
name = "faym-dashboard"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.2",
    "sqlalchemy>=2.0.51",
    "uvicorn>=0.51.0",
    "psycopg[binary]>=3.2",
]
